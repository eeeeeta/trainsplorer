FROM debian:stable-slim AS tspl-fahrplan
WORKDIR /tspl
RUN apt-get update && apt-get install -y libssl1.1 ca-certificates sqlite3
COPY --from=tspl-compiled /tspl/target/release/tspl-fahrplan /tspl
ENTRYPOINT ["/tspl/tspl-fahrplan"]

